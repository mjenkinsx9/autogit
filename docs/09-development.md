# Development

```bash
npm test    # node --test test/*.test.js
```

Node 18+, zero dev dependencies — tests use the built-in `node:test` runner (83 tests). CI runs them on Node 18/20/22 across Linux and macOS.

See [AGENTS.md](../AGENTS.md) for working rules, response style, and the release process.

---

Back to the documentation index: [README.md](README.md)
